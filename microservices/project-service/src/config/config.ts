import dotenv from 'dotenv';nndotenv.config();nnexport const config = {n  port: parseInt(process.env.PORT || '3005', 10),n  nodeEnv: process.env.NODE_ENV || 'development',n  n  // Databasen  databaseUrl: process.env.DATABASE_URL || 'postgresql://user:password@localhost:5432/devsync_db',n  n  // Telemetryn  telemetryEnabled: process.env.TELEMETRY_ENABLED === 'true',n  jaegerAgentHost: process.env.JAEGER_AGENT_HOST || 'localhost',n  jaegerAgentPort: parseInt(process.env.JAEGER_AGENT_PORT || '6832', 10),n  prometheusPort: parseInt(process.env.PROMETHEUS_PORT || '9469', 10)n};
