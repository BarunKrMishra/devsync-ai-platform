import winston from 'winston';nnconst logger = winston.createLogger({n  level: process.env.LOG_LEVEL || 'info',n  format: winston.format.combine(n    winston.format.timestamp(),n    winston.format.errors({ stack: true }),n    winston.format.json()n  ),n  defaultMeta: { service: 'project-service' },n  transports: [n    new winston.transports.Console({n      format: winston.format.combine(n        winston.format.colorize(),n        winston.format.simple()n      )n    })n  ]n});nnexport default logger;
